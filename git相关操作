git add .                             ---->将文件添加到缓存区
git commit -m "提交说明"              ---->将缓存区内容添加到本地仓库
git push                              ---->将本地仓库内容提交到远程仓库

git log                               ---->可以查看commit id 和提交说明
git reset --hard HEAD^                ---->^表示上一个版本,^^表示上面两个,~30表示30个版本
git reset --hard + commit id          ---->回退版本

git branch                            ---->查看当前分支,分支前有*代表当前分支
git checkout +分支名                  ---->切换到当前分支

git clone + url                       ---->从远程仓库克隆项目代码
git checkout -b + 分支名              ---->创建并切换到该分支

git remote                            ---->查看远程库信息

git status                            ---->查看项目的当前状态
git diff                              ---->查看执行git status的结果详细信息

git merge                             ---->将其他分支合并到当前分支中去
git pull                              ---->取回远程主机某个分支的更新，再与本地的指定分支合并
git pull <远程主机名> <远程分支名>:<本地分支名>    ---->如果是使用git clone，建立了追踪关系，可以省略主机名
 
合并分支会产生冲突，需要手动解决冲突，解决后用 git add告诉 Git 文件冲突已经解决
