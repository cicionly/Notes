event.target  当前事件对象
event.target.value 当前对象的值
event.keyCode   从键盘获取的键码
1、screenX/screenY 是以屏幕为基准进行测量，即：当前元素距离屏幕的尺寸
2.pageX 和 pageY 是以当前文档（绝对定位）为基准，不适用于IE6-8;会计算滚动长度
3.clientX 和 clientY 是以当前可视区域为基准，类似于固定定位。会忽略滚动长度
